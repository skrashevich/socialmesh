# Minimal environment file for CI builds
# All values either empty or match hardcoded defaults in the app.
# dotenv.load() requires a non-empty file to exist.

# RevenueCat (disabled in CI)
REVENUECAT_IOS_API_KEY=
REVENUECAT_ANDROID_API_KEY=
